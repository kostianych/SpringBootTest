spring:
  datasource:
    driver-class-name: com.microsoft.sqlserver.jdbc.SQLServerDriver
    url: jdbc:sqlserver://localhost:1433;database=Customer;
    username: sa
    password: ^hXLgBwYWT
  logging:
    level: TRACE
  jpa:
    show-sql: true
  properties:
    hibernate:
      format_sql=true: